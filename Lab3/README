Название лаб2 осталось от прошлой
3 лабораторная предствлена в библиотеке СonfigManager(СlassLibrary1).
Основным классом является Logger.Именно его объект создаётся в начале службы,передавая путь для нужной конфигурации.В Logger на основании полученного путя создаётся объекты классов,
предназначенные для извлечения конфигурации из файла соответствующего типа. Затем поочередно мы отсылаемся к классам этих объектов, 
что бы найти нужный файл конфигурации,который в свою очередь перерабатывается своим парсером и отдает данные в программу
Интерфейс IBase создаёт точку расширения,гарантируя,что классы,извлекающие данные из файлов, имеют метод по выдаче данных.
Класс JsonReader предназначен для извлечения информации из json файла.При это он не использует Newtonsoft.Json или System.Text.Json.Есть возможность добавления новых полей 
в блоках конфигурации с соответствующими изменениями модели на стороне .NET.
Класс XMLReader предназначен для извлечения информации из xml файла.Есть проверка валидности xml через xsd.Есть возможность добавления новых полей в блоках конфигурации с 
соответствующими изменениями модели на стороне .NET.
Класс StrEnter представляет из себя блоки конфигурации и содержит свойства описывающие соответственные пути.Есть возможность добавления новых полей.
Исходники файлов с кофигами по стандарту находятся на диске D без папок, ttt используется для тестов, архивированный файл вместе с итоговым находятся в последнем пути
